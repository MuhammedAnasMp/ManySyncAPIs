account_template = AccountTemplate.objects.get(
    account=account,
    template_type='reel'
)

template = account_template.template
config = AccountTemplateConfiguration.objects.get(
    account=account,
    template_type='reel'
)




final_post = {
    "design": template.template_json if template else None,
    "config": config.configuration
}


3. 🧩 When template is deleted

Because:

on_delete=models.SET_NULL

👉 This happens:

AccountTemplate.template = NULL

BUT:

AccountTemplateConfiguration ✅ still exists



4. 🎛️ In your frontend

When loading editor:

Fetch selected template via AccountTemplate
Fetch config via AccountTemplateConfiguration
Example API response:
{
  "template": {
    "id": 12,
    "type": "reel"
  },
  "configuration": {
    "default_caption": "Hello 🔥",
    "default_hashtags": ["#fun"]
  },
  "features": {
    "caption": true,
    "hashtags": false
  }
}